# -*- mode: shell-script -*-

# TODO: Before enabling this config script, fill in where the mongod source is found in the if expression

# if [ -d mongod ] ; then
#   alias mongup='mongod --config /usr/local/etc/mongod.conf'
#   alias mongdn='echo "Use db.shutdownSever() from mongo cli" && echo "See https://docs.mongodb.com/manual/tutorial/manage-mongodb-processes/#use-shutdownserver"'
#   alias mongst='mongostat'
# if
